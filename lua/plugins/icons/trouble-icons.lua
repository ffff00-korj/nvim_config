local sign_icons = require("plugins.icons.sign-icons")

local icons = {
    error = sign_icons.error,
    warning = sign_icons.warn,
    hint = sign_icons.hint,
    information = sign_icons.info,
    other = sign_icons.other,
}

return icons
